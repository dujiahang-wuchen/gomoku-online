{
  "name": "gomoku-online",
  "version": "1.0.0",
  "private": true,
  "description": "一个支持本地、人机和好友房间对战的五子棋小游戏。",
  "scripts": {
    "start": "node server.js",
    "check": "node --check script.js && node --check server.js && node --check tunnel.js",
    "tunnel": "cloudflared tunnel --url http://localhost:5173",
    "tunnel:local": "node tunnel.js"
  },
  "dependencies": {
    "cloudflared": "^0.7.1",
    "localtunnel": "^2.0.2",
    "ws": "^8.20.0"
  }
}
